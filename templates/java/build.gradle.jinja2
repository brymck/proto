buildscript {
    repositories {
        maven {
            url "https://jcenter.bintray.com"
        }
    }
    dependencies {
        classpath 'com.bmuschko:gradle-nexus-plugin:2.3.1'
    }
}

plugins {
    id 'java-library'
    id 'maven-publish'
    id 'com.bmuschko.nexus' version '2.3.1'
}

archivesBaseName = 'brymck-{{ artifactName }}'
group 'com.github.brymck'
version '{{ artifactVersion }}'

repositories {
    maven {
        url "https://jcenter.bintray.com"
    }
}

modifyPom {
    project {
        name 'brymck-{{ artifactName }}'
        description 'Some description'
        url 'https://github.com/brymck/proto'
        inceptionYear '2020'

        scm {
            url 'https://github.com/brymck/proto'
            connection 'scm:git:git://github.com/brymck/proto.git'
            developerConnection 'scm:git:git@github.com:brymck/proto.git'
        }

        licenses {
            license {
                name 'MIT License'
                url 'opensource.org/licenses/mit-license.php'
                distribution 'repo'
            }
        }

        developers {
            developer {
                id 'brymck'
                name 'Bryan McKelvey'
                email 'bryan.mckelvey@gmail.com'
                url 'https://github.com/brymck'
            }
        }
    }
}

dependencies {
    implementation 'com.google.protobuf:protobuf-java:{{ protobufVersion }}'
{%- if hasServices %}
    implementation 'io.grpc:grpc-netty-shaded:{{ grpcVersion }}'
    implementation 'io.grpc:grpc-protobuf:{{ grpcVersion }}'
    implementation 'io.grpc:grpc-stub:{{ grpcVersion }}'
{%- endif %}
{%- for dependency in dependencies %}
    compile group: 'com.github.brymck', name: '{{ dependency }}', version: '{{ artifactVersion }}'
{%- endfor %}
}

extraArchive {
    sources = true
    tests = true
    javadoc = true
}

nexus {
    sign = true
    repositoryUrl = 'https://oss.sonatype.org/service/local/staging/deploy/maven2/'
    snapshotRepositoryUrl = 'https://oss.sonatype.org/content/repositories/snapshots/'
}
